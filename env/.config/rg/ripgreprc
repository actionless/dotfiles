# follow symbolic links:
--follow

# Search binary files as if they were text:
--text

# ignore patterns:
--glob
!*.png
--glob
!*.jpg
--glob
!*.jpeg
--glob
!*.so
--glob
!*.pyc
--glob
!.git/*
--glob
!env/*

# case insensitive unless caps letter used:
--smart-case
